PORT=4000
NODE_ENV=development

JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Neon Postgres (get both from Neon dashboard > Connection Details)
# DATABASE_URL = Pooled connection (runtime) — ends with -pooler... + ?sslmode=require
# DIRECT_URL   = Direct connection (migrations/seed) — no pooler
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.c-2.us-east-2.aws.neon.tech/bovipulse?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxx.c-2.us-east-2.aws.neon.tech/bovipulse?sslmode=require"

# Local dev: http://localhost:5173
# Production on Render: https://bovipulse.vercel.app (comma-separate for multiple origins, no trailing slash)
CORS_ORIGIN=http://localhost:5173

UPLOAD_DIR=uploads
MAX_UPLOAD_MB=5

THERMAL_FEVER_THRESHOLD_C=39.5
